# Price Utilities Library

This library contains several utility-style pricing functions used across different systems at PriceFixers.

The goal is to reuse pricing logic wherever possible.

## TODO

The `calculate_total` function needs some improvements:

- (task-1) Explicitly round to 2 decimal places

- (task-2) Add support for a discount (default value of zero)

Make whatever changes are necessary to get this working.

## Instructions

Clone the repository, implement the changes, and push directly to `main`.

## Licensing

For licensing questions, contact Jim at jim@pricefixers.com 